[project]
name = "talent-mineru-runtime"
version = "0.1.0"
description = "Local private MinerU runtime for complex talent documents"
requires-python = ">=3.12,<3.13"
dependencies = ["mineru[core]>=2.6.4"]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
